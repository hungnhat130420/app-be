
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/employee_system
    username: postgres
    password: 123456

  jpa:
    hibernate:
      show-sql: true
      ddl-auto: update

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
